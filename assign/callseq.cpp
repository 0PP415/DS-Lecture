#include <iostream>
#include <vector>
#include <deque>
#include <map>

using namespace std;

int main(void) {
    int N, k1, k2;
    cin >> N >> k1 >> k2;

    vector<string> seq;    
    map<string, vector<string>> functions;

    return EXIT_SUCCESS;
}